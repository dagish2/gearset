({
    doInit : function(component, event, helper) {
        helper.getUserTimeZoneOffset(component);
    },
    showClock : function(component, event, helper) {
        helper.showClock(component);
    }
})